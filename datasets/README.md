Datasets included here are the benchmark-ready property-graph inputs and prepared partitioning artifacts used by the final native Docker pipeline.

Prepared graph inputs are stored under `partition_prep/`.
CORDIS also includes `rcp_neo4j_csv/` for the RCP path.
CORD-19 full RCP uses `cord19_full_typed_light_inputs`.
